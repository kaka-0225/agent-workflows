# Agent Templates

Agents define specialist roles.

Use an agent when a task needs:

- A stable role
- Clear scope and non-scope
- Tool boundaries
- A dedicated model or effort level
- Preloaded skills
- Structured output

Agent design principle:

```text
Agent is the specialist. Command is the coordinator. Skill is the method.
```
